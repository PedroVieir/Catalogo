# Configuração do Banco de Dados
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=sua_senha
DB_NAME=abr_catalogo
DB_TIMEZONE=+00:00

# Configuração do Servidor
PORT=4000
NODE_ENV=development

# Configuração do Pool de Conexões (valores padrão são usados se não definidos)
# DB_CONNECTION_LIMIT=10        # Máximo de conexões ativas simultâneas (padrão: 10)

# CORS (quando pronto para produção)
# CORS_ORIGIN=https://seu-dominio.com
